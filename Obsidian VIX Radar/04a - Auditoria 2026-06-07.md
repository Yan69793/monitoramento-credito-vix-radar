# Auditoria de Produção — 2026-06-07

Tipo: auditoria completa multi-camada (7 subagentes paralelos).
Executada: 2026-06-07T08:05–08:10Z.
Branch: `audit/reconcile-prod-2026-06-01`.

## Estado confirmado

- Worker: v4.9.101 (prod == repo — sem drift)
- Frontend: v201.44 em prod / v201.45 no repo — deploy Pages pendente
- Telemetria: OK (`bindings.telemetria:true`)
- Cascade: 3/3
- KV/DO: up
- CORS apex: OK | CORS www: OK (fix v4.9.101 confirmado)
- Auth POST /: OK (401 em 0.07s)
- Crons: 4/4 corretos no wrangler.toml
- Multi-semana: OK (5 endpoints confirmados com `carregarEstadoMultiSemana(env,5)`)

## Achados

| Severidade | Achado |
|---|---|
| ALTO | `version.json` prod = `v201.44`, HTML prod = `v201.45` — auto-update dispara em loop |
| MÉDIO | Deploy Pages pendente — repo `v201.45` à frente de prod `v201.44` |
| MÉDIO | `app/deploy_zip/index.html` = `v201.44` (desatualizado vs `app/index.html` = `v201.45`) |
| MÉDIO | 204 arquivos `D` + nova estrutura (`api/`, `app/`) não commitada no git |
| BAIXO | CORS fallback para origin não-permitida retorna apex em vez de omitir header |
| INFO | Propósito cron `0 2 * * *` não documentado |

## Drift

| Componente | Repo | Prod | Drift |
|---|---|---|---|
| Worker | v4.9.101 | v4.9.101 | Nenhum |
| Frontend (index.html) | v201.45 | v201.44 | Repo à frente — deploy pendente |
| Frontend (deploy_zip) | v201.44 | v201.44 | Sincronia com prod, defasado do repo |
| Estrutura de diretórios | api/, app/ (não commitados) | — | Reorganização pendente de commit |

## Causa raiz / Correção / Validação

Causa raiz (version.json divergente): `app/index.html` atualizado para `v201.45` mas deploy do Pages não foi executado. Script de build (`deploy_zip`) também não foi rodado.

Correção aplicada: nenhuma (auditoria read-only).

Validação em produção: todos os testes de health, CORS, auth e version.json executados ao vivo em 2026-06-07T08:05Z. Worker íntegro. Frontend com divergência ativa confirmada.

## Pendências e próximo passo

1. Deploy Pages v201.45 — sincronizar deploy_zip + executar `npx wrangler pages deploy ./app/deploy_zip` — Yan
2. Commitar reorganização api/app no git — `git add api/ app/ ... && git rm -r --cached worker/ ...` — Yan
3. Verificar CSS `<strong>` global sem `color` no HTML — grep em `app/index.html`
4. Documentar propósito do cron `0 2 * * *` no wrangler.toml

## Evidência bruta (principal)

```
GET https://radar-credito-api.prospects-intel.workers.dev
{"ok":true,"versao":"v4.9.101","ts":"2026-06-07T08:06:54.528Z",
 "bindings":{"kv":true,"rate_limiter":true,"telemetria":true},
 "providers_configurados":"3/3"}
HTTP:200 TEMPO:0.174748s

https://vixradar.com/version.json
{"version":"v201.44","deployed_at":"2026-06-05T11:31:01Z"}
HTTP:200

CACHE_VERSION do HTML apex: CACHE_VERSION="v201.45"

CORS www: Access-Control-Allow-Origin: https://www.vixradar.com
CORS apex: Access-Control-Allow-Origin: https://vixradar.com
POST / anônimo: HTTP:401 TEMPO:0.071721s
```
